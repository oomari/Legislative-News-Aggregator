- Create the models
  - NewsArticles (done)
- Create routes:
  - GET /news
    [x] query params: state, topic, search
    [x] returns list of news articles
  - GET /news/:id
    [x] returns single news article
  - POST /news (don't do)
